# Screenshots — TODO

Real captures of the app are pending. The AppStream metainfo
(`resources/app.unisic.UnisicStudio.metainfo.xml`) references:

- `editor.png` — the editor with auto zoom, styling and the timeline
- `export.png` — the export dialog (MP4 / WebM / GIF)

Capture these on a live Wayland session (the offscreen QPA plugin can't
initialize the GL RHI the composition needs) and drop the PNGs here, then
update the metainfo/README links.
